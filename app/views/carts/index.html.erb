<% @products.each do |product| %>
    <li>
      <div><%= product.name %> | <%= product.price %>$</div>
      <div></div>
    </li>
  <% end %>
</ul>
<%= link_to "Confirm Purchase", cart_path(id: @cart.id) %>